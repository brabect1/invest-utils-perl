sudo apt-get install libdbd-sqlite3-perl sqlite3
cpan Getopt::Long::Descriptive
sudo cpan Switch
sudo cpan Array::Utils
sudo apt-get install libwww-perl build-essential
#sudo cpan install LWP:UserAgent
#sudo cpan install Date::Manip
#sudo cpan install Package::Stash::XS
#sudo cpan install Package::Stash
#sudo cpan install DateTime
sudo cpan install Finance:Quote
export PERL5LIB=$PERL5LIB:/home/mint/perl5/lib/perl5
touch ~/.vimrc
echo "filetype plugin indent on" >> ~/.vimrc
echo "\" show existing tab with 4 spaces width" >> ~/.vimrc
echo "set tabstop=4" >> ~/.vimrc
echo "\" when indenting with '>', use 4 spaces width" >> ~/.vimrc
echo "set shiftwidth=4" >> ~/.vimrc
echo "\" On pressing tab, insert 4 spaces" >> ~/.vimrc
echo "set expandtab" >> ~/.vimrc
echo "\" enable line numbers" >> ~/.vimrc
echo "set number" >> ~/.vimrc
